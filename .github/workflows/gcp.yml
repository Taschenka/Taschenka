---
name: Build and Push Images to Google Cloud Platform
on:
  push:
    branches: [master]
jobs:
  build-push-gcr:
    name: Build and Push to GCP
    runs-on: ubuntu-latest
    env:
      PROJECT_ID: taschenka
      TASCHENKA_API: taschenka_api
      TASCHENKA_WEB: taschenka_web
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - uses: google-github-actions/setup-gcloud@main
        with:
          service_account_key: ${{ secrets.SERVICE_ACCOUNT_KEY }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Build Docker Images
        run: |-
          docker build -t $TASCHENKA_API:latest ./Api/src
          docker build -t $TASCHENKA_WEB:latest ./Api/src

      - name: Configure Docker Client
        run: |-
          gcloud auth configure-docker

      - name: Push Docker Images to GCR
        env:
          GIT_TAG: v1.2.0
        run: |-
          docker tag $TASCHENKA_API:latest gcr.io/$PROJECT_ID/$TASCHENKA_API:latest
          docker tag $TASCHENKA_API:latest gcr.io/$PROJECT_ID/$TASCHENKA_API:$GIT_TAG
          docker push gcr.io/$PROJECT_ID/$TASCHENKA_API:latest
          docker push gcr.io/$PROJECT_ID/$TASCHENKA_API:$GIT_TAG
          docker tag $TASCHENKA_WEB:latest gcr.io/$PROJECT_ID/$TASCHENKA_WEB:latest
          docker tag $TASCHENKA_WEB:latest gcr.io/$PROJECT_ID/$TASCHENKA_WEB:$GIT_TAG
          docker push gcr.io/$PROJECT_ID/$TASCHENKA_WEB:latest
          docker push gcr.io/$PROJECT_ID/$TASCHENKA_WEB:$GIT_TAG
